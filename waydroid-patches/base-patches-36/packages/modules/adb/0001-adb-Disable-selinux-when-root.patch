From f9a60d053ddcdc3d4cb81776148b515bff424f0e Mon Sep 17 00:00:00 2001
From: Alessandro Astone <ales.astone@gmail.com>
Date: Thu, 15 Jan 2026 16:51:19 +0000
Subject: [PATCH 1/2] adb: Disable selinux when root

Change-Id: Ifbbffb6012e02161751ca636484b6e9346c12875
---
 daemon/main.cpp | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/daemon/main.cpp b/daemon/main.cpp
index 87e56dd..c4bed22 100644
--- a/daemon/main.cpp
+++ b/daemon/main.cpp
@@ -173,7 +173,8 @@ static void drop_privileges() {
         // minijail_enter() will abort if any priv-dropping step fails.
         minijail_enter(jail.get());
 
-        if (root_seclabel != nullptr) {
+        // Disabled in Waydroid
+        if (false /* root_seclabel != nullptr */) {
             if (selinux_android_setcon(root_seclabel) < 0) {
                 // If we failed to become root, don't try again to avoid a
                 // restart loop.
-- 
2.55.0

