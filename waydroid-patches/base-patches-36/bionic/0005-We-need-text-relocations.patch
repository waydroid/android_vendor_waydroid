From 0481de1519afe99e24fa7b27483468bd548fbb0c Mon Sep 17 00:00:00 2001
From: Chih-Wei Huang <cwhuang@linux.org.tw>
Date: Fri, 27 Feb 2026 07:12:09 +0000
Subject: [PATCH 5/6] We need text relocations

Don't show annoying warnings.

Change-Id: Ied04dda12a03ccdfd3d4be6d81e8d7d27a8ce9e0
---
 linker/linker.cpp | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/linker/linker.cpp b/linker/linker.cpp
index 5c70a40..5e37bf3 100644
--- a/linker/linker.cpp
+++ b/linker/linker.cpp
@@ -3394,8 +3394,9 @@ bool soinfo::link_image(const SymbolLookupList& lookup_list, soinfo* local_group
     if (DL_ERROR_AFTER(23, "\"%s\" has text relocations", get_realpath())) {
       return false;
     }
-#endif
+
     add_dlwarning(get_realpath(), "text relocations");
+#endif
     // Make segments writable to allow text relocations to work properly. We will later call
     // phdr_table_protect_segments() after all of them are applied.
     if (phdr_table_unprotect_segments(phdr, phnum, load_bias, should_pad_segments_,
-- 
2.55.0

